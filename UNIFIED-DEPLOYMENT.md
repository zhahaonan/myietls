# MyIELTS Voice 统一部署指南

## 🏗️ 部署架构
```
前端React → 静态构建 → 与后端打包 → Docker镜像 → 魔搭创空间
    ↓          ↓           ↓           ↓          ↓
  Vite构建   dist目录    统一容器    单一部署    云端运行
```

## 📦 部署流程

### 1. 项目结构
```
myielts-voice-05/
├── frontend/           # React前端源码
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
├── backend/            # Python后端源码
│   ├── app.py         # 主应用文件
│   ├── engine/        # AI引擎模块
│   ├── dist/          # 前端静态文件（构建后）
│   └── requirements.txt
├── build.sh           # Linux/Mac构建脚本
├── build.bat          # Windows构建脚本
└── MyIELTS_Voice/     # 魔搭创空间部署目录
```

### 2. 构建步骤

#### 自动构建（推荐）
```bash
# Linux/Mac
./build.sh

# Windows
build.bat
```

#### 手动构建
```bash
# 1. 构建前端
cd frontend
npm install
npm run build

# 2. 复制静态文件
cp -r dist ../backend/

# 3. 构建Docker镜像
cd ../backend
docker build -t myielts-voice .
```

### 3. Docker部署配置

**Dockerfile关键特性**：
- 基于魔搭创空间Python 3.10镜像
- 前后端统一打包
- 静态文件服务支持
- 非root用户安全运行

**环境变量配置**：
```bash
API_KEY=your_gemini_api_key
DASHSCOPE_API_KEY=your_dashscope_api_key
```

### 4. 魔搭创空间部署

#### 部署前准备
1. 确保所有代码已推送到魔搭创空间仓库
2. 在平台设置环境变量
3. 触发自动构建

#### 访问方式
- **Gradio界面**：`http://your-app.modelscope.cn`
- **前端应用**：`http://your-app.modelscope.cn/app`
- **API服务**：`http://your-app.modelscope.cn/api/`

### 5. 技术优势

#### 统一部署
- ✅ 单一Docker镜像包含前后端
- ✅ 简化部署流程
- ✅ 统一环境管理

#### 性能优化
- ✅ 静态资源预构建
- ✅ 前端资源缓存
- ✅ 服务端渲染支持

#### 安全性
- ✅ 非root用户运行
- ✅ 环境变量隔离
- ✅ API密钥保护

### 6. 开发与生产环境

#### 开发环境
```bash
# 前端开发
cd frontend
npm run dev  # 端口3000

# 后端开发
cd backend
python app.py  # 端口7860
```

#### 生产环境
```bash
# 统一部署
docker run -p 7860:7860 myielts-voice
```

### 7. 故障排除

#### 常见问题
1. **前端资源未加载**：检查dist目录是否存在
2. **API连接失败**：验证环境变量配置
3. **权限问题**：确认Dockerfile用户配置

#### 调试命令
```bash
# 查看容器日志
docker logs <container_id>

# 进入容器调试
docker exec -it <container_id> /bin/bash

# 检查静态文件
ls /home/user/app/dist/
```

这套统一部署方案将前后端完全整合，通过单一Docker镜像实现简化部署，特别适合魔搭创空间的部署环境。